#include <stdio.h>
#include <unistd.h>
#include <fcntl.h>

int main() {
  //char* s = (char*)malloc(99);
  int n = 0;
  scanf("%d", &n);
  printf("%d", n);
  //char c = getc(stdin);
  //write(1, "qwe", 3);
  //open("utils.c", O_RDONLY, 1);
  return 0;
}
